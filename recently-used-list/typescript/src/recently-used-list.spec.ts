import { describe, it } from 'vitest';
import { RecentlyUsedList } from './recently-used-list';

describe('RecentlyUsedList', () => {
  it('can be constructed', () => {
    new RecentlyUsedList();
  });
});
